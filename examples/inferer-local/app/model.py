"""Example inference model.

Replace the body of run_model with your own prediction logic.
The nonce is the block height for the current epoch.
"""


def run_model(nonce: int) -> float:
    return 10.0
